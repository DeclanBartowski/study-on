{% extends 'base.html.twig' %}

{% block title %}Изменить курс {{ course.name }}{% endblock %}

{% block body %}
    <h1>Изменить курс {{ course.name }}</h1>

    {{ include('course/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_course_index') }}">Вернуться к списку</a>

    {{ include('course/_delete_form.html.twig') }}
{% endblock %}
