{% extends 'base.html.twig' %}

{% block title %}Изменить урок "{{ lesson.name }}"{% endblock %}

{% block body %}
    <div class="container">
        <p class="h1">Изменить урок "{{ lesson.name }}"</p>
        <p class="lead text-body-secondary">Курс <a href="{{ path('app_course_show', {'id': course.id}) }}">{{ course.name }}</a></p>
        {{ include('lesson/_form.html.twig', {'button_label': 'Обновить'}) }}
        <br>
        {{ include('lesson/_delete_form.html.twig') }}

        <hr class="my-4">
        <a class="btn btn-secondary" href="{{ path('app_lesson_index') }}">Вернуться к списку</a>
    </div>
{% endblock %}
