{% extends 'base.html.twig' %}

{% block title %}Изменить урок "{{ lesson.name }}"{% endblock %}

{% block body %}
    <h1>Изменить урок "{{ lesson.name }}"</h1>

    {{ include('lesson/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_lesson_index') }}">Вернуться к списку</a>

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
